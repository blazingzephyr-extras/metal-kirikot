const Bot = require("F:/Metal Kirikot/bot")


Bot.on('ready', async  () => {
    /*
    Bot.user.setActivity(`being created🛠`, {type: 'WATCHING'});
    Bot.generateInvite(["ADMINISTRATOR"]).then(link => console.log(link))
    
    Bot.guilds.forEach((server) => {
        console.log(`\n`),
        console.log(`Bot is invited on this server:`),
        console.log(server.name),
        console.log(`\n`),
        console.log(`${server.name}'s channels:`),
        server.channels.forEach((channel) => {
                console.log(` - ${channel.name}     Type: ${channel.type}   ID: ${channel.id}  Position: ${channel.position}   ${channel.permissionOverwrites}  ${channel.createdAt}`)
        })
        server.roles.forEach((role) => {
            console.log(` - ${role.name}   ID: ${role.id}  `)
        })
    })
    */
})